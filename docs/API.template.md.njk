{# API reference template — DO NOT edit docs/API.md directly.
   Rendered to ../API.md by docs/scripts/build-docs.mjs.

   Variables provided to the template:
     - pkg     : package.json contents
     - modules : ApiModule[] from docs/scripts/extract-api.mjs
#}
# {{ pkg.name }} — API reference

> Generated from JSDoc by [`docs/scripts/build-docs.mjs`](./scripts/build-docs.mjs)
> on every `npm run build:docs` run. Do not edit this file by hand.
> **v{{ pkg.version }}** · {{ pkg.license }}

This is the auto-generated API reference for `{{ pkg.name }}`. The
shape of the public API is documented in [`README.md`](../README.md);
this document is the exhaustive view across every exported symbol in
[`index.mjs`](../index.mjs) and [`src/`](../src/).

## Table of contents

{% for m in modules -%}
- [`{{ m.name }}`](#{{ m.name | replace("/", "") | replace("(", "") | replace(")", "") | replace(" ", "-") | lower }})
{% endfor %}

---

{% for m in modules %}
## `{{ m.name }}`

_Source: [`{{ m.relPath }}`]({{ m.relPath | replace(" ", "%20") }})_

{% if m.description %}{{ m.description }}{% endif %}

{% if m.symbols.length > 0 %}
### Exports

| Name | Kind | Visibility |
| ---- | ---- | ---------- |
{%- for s in m.symbols %}
| [`{{ s.name }}`](#{{ s.name | lower }}-{{ m.name | replace("/", "") | replace("(", "") | replace(")", "") | replace(" ", "-") | lower }}) | {{ s.kind }}{% if s.async %} (async){% endif %} | {{ s.visibility }} |
{%- endfor %}

{% for s in m.symbols %}
#### <a id="{{ s.name | lower }}-{{ m.name | replace("/", "") | replace("(", "") | replace(")", "") | replace(" ", "-") | lower }}"></a>`{{ s.name }}`

{% if s.deprecated %}> ⚠️ **Deprecated:** {{ s.deprecated }}{% endif %}

```ts
{{ s.signature }}
```

{% if s.description %}{{ s.description | trim }}{% endif %}

{% if s.templates.length > 0 %}
**Type parameters:** {% for t in s.templates %}`{{ t }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}

{% if s.params.length > 0 %}
**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
{%- for p in s.params %}
| `{{ p.name }}`{% if p.optional %} _(optional)_{% endif %} | {% if p.type %}`{{ p.type }}`{% else %}—{% endif %} | {{ p.description | replace("\n", " ") | trim or "—" }} |
{%- endfor %}
{% endif %}

{% if s.returns %}
**Returns:** {% if s.returns.type %}`{{ s.returns.type }}`{% endif %}{% if s.returns.description %} — {{ s.returns.description | replace("\n", " ") | trim }}{% endif %}
{% endif %}

{% if s.throws.length > 0 %}
**Throws:**
{% for t in s.throws %}- {{ t }}
{% endfor %}{% endif %}

{% if s.examples.length > 0 %}
**Example**

{% for ex in s.examples %}```js
{{ ex | trim }}
```

{% endfor %}{% endif %}

{% if s.kind == "class" and s.members and s.members.length > 0 %}
**Members**

{% for member in s.members %}
##### `{{ s.name }}.{{ member.name }}`

```ts
{{ member.signature }}
```

{% if member.description %}{{ member.description | trim }}{% endif %}

{% if member.params.length > 0 %}
| Param | Type | Description |
| ----- | ---- | ----------- |
{%- for p in member.params %}
| `{{ p.name }}`{% if p.optional %} _(optional)_{% endif %} | {% if p.type %}`{{ p.type }}`{% else %}—{% endif %} | {{ p.description | replace("\n", " ") | trim or "—" }} |
{%- endfor %}
{% endif %}

{% if member.returns %}
**Returns:** {% if member.returns.type %}`{{ member.returns.type }}`{% endif %}{% if member.returns.description %} — {{ member.returns.description | replace("\n", " ") | trim }}{% endif %}
{% endif %}

{% endfor %}{% endif %}

{% endfor %}
{% endif %}

{% if m.typedefs.length > 0 %}
### Type definitions

{% for t in m.typedefs %}
#### `{{ t.name }}`

```ts
{{ t.raw }}
```

{% if t.description %}{{ t.description | trim }}{% endif %}

{% endfor %}
{% endif %}

---

{% endfor %}

_Total: {{ modules.length }} modules._
